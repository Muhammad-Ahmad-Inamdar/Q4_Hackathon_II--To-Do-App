// This file can be removed or kept for potential future use
// Currently, we're handling auth directly through API calls to the backend
// Placeholder to prevent import errors if needed elsewhere
export const authClient = null;